services:
  api:
    container_name: qt_bank_api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5044:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    restart: always
